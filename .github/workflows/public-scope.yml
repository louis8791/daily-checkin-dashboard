name: Public scope guard

on:
  push:
  pull_request:

jobs:
  guard:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - name: Reject private-data paths and export formats
        shell: pwsh
        run: ./scripts/check-public-scope.ps1
