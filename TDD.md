# TDD：驗收與防錯規格

## 狀態定義

- `PASS`：測試在指定入口實際完成，結果與證據可重跑。
- `FAIL`：結果不符規格或有資料／隱私風險。
- `SKIPPED`：本輪未執行，不得冒充通過。
- `OWNER-ONLY`：必須由 Owner 使用真實裝置、帳號或姓名完成；AI 不接觸真實資料。

## 日期與週次

1. 8/18～8/24 為第1週，10/6～10/12 為第8週。
2. 8/18 至 10/12 含首尾共 56 天。
3. 進行中週次的總次數只計至統計截至日。
4. 尚未開始的週次在折線圖中為空值，不得寫入 0。
5. 台灣時間 23:59 為日界線；時區轉換不得把紀錄移到相鄰日期。

## 接龍解析

使用合成名冊測試，不把真實姓名交給 AI；正式頁面使用 Owner 在本機設定的名冊。

1. 編號 `1.`、`1、`、全形空白及前後空白皆可解析。
2. 每日標題、每週任務及標語不被當成姓名。
3. 同一姓名同日出現兩次，只產生一次完成紀錄並提示重複。
4. 不在名冊的文字列為未知內容，確認按鈕停用。
5. 空白貼文或只有標題時不得寫入。
6. 名冊中的一部分姓名沒出現時，列為「今日尚未接龍」。
7. 接龍文字中的 `YYYY/MM/DD`、`YYYY-MM-DD`、`M/D` 或 `M月D日` 可判斷日期；找不到日期時必須提示確認，不得自行猜測。
8. 直接貼上含標語、日期／星期行、編號姓名與外層 `[]` 的群組原文時，只取編號姓名，末尾括號不會併入姓名。

## 入帳與補登

1. 第一次確認日期 D，建立一份日紀錄。
2. 同一日期再次確認，更新原紀錄，不新增第二份日紀錄。
3. 事後補入姓名後，該日、該週、個人累計、排行及 PDF 資料同步重算。
4. 補登後不顯示遲交標籤。
5. 已確認來源中沒有姓名時才計該人當日完成次數為 0。
6. 日期完全沒有匯入來源時顯示 `source_missing`，不得推論 10 人皆為 0。
7. 每次更新保存時間與前後差異，原始資料與解析結果可追溯。

## 達成率

1. 完整週完成 7 天顯示完成7次、總次數7次、達成率100%。
2. 完整週完成 5 天顯示完成5次、總次數7次、達成率71.4%。
3. 第3週第1天完成 1 次，顯示完成1次、總次數1次、達成率100%。
4. 八週完成後總次數固定 56。
5. 分母為 0 時顯示尚未開始，不可輸出 NaN、Infinity 或 0%。

## 圖表與排行

1. 個人折線圖 x 軸固定第1～8週，未開始週為空白。
2. 週次切換後，表格、折線圖、長條圖與標題必須一致。
3. 完成次數相同者並列同名次，顯示順序使用固定名冊順序。
4. 進行中排行標示「截至目前」，不得冒充最終結果。
5. 手機窄版可閱讀所有姓名、週次、數字與圖例，不裁切主要操作。

## PDF

1. 即時總覽、指定週、指定個人、每日紀錄及完整報告皆能獨立匯出。
2. PDF 數字、表格及圖表與畫面同一資料快照一致。
3. PDF 包含統計期間、截至時間、最後更新時間與產生時間。
4. 中文字不缺字、圖表不被分頁裁斷、長表能正確續頁。
5. 手機 Safari／Chrome 與桌面瀏覽器實際下載、開啟、分享皆需 Runtime Acceptance。
6. PDF 由瀏覽器產生；網路記錄不得出現把姓名送往 PDF 或 AI 服務的請求。
7. PDF 產生失敗時顯示原因，不下載空白或過期檔案。

## 權限與加密

1. 未驗證訪客不能取得姓名、統計、密文解密金鑰或 API 明文回應。
2. 查看者只能讀取，所有新增、修改與刪除入口必須拒絕。
3. 管理者操作需要獨立驗證，不和查看密碼共用寫入權限。
4. 正式資料在送出裝置前完成加密；伺服器、log、分析與錯誤回報只可看到密文或不含個資的狀態。
5. 搜尋引擎索引規則不是權限控制；即使知道網址，未驗證仍不能查看。
6. 復原金鑰流程為 `OWNER-ONLY`，不可交給 AI 或寫入 repo。

## 公開範圍防護

1. `private-data` 位於 `E:\打卡\repo` 外。
2. `pwsh -File scripts/check-public-scope.ps1` 必須通過。
3. 每次 commit 前檢查 tracked／staged 檔，不得含名冊、接龍、統計、PDF、截圖、資料庫、試算表、備份或金鑰。
4. CI 不得上傳正式資料 artifacts。
5. 公開文件與測試只可使用明確假名及合成數值。

## 鼓勵性用詞

- 群組頁只使用：今日已接龍、今日尚未接龍、完成次數、總次數、達成率、繼續累積、累積次數排行。
- 禁止：應完成、未達標、失敗、缺席、不合格、懲罰或催促語言。
- 顏色不得以紅色羞辱或標記個人。

## 端到端硬閘

以完全假名資料完成：手機管理者登入 → 選日期 → 貼上 → 異常預覽 → 確認 → 群組唯讀手機重新整理 → 週次與個人圖表更新 → 匯出 PDF → 補登 → 全部統計與 PDF 重算。任何一步未實際完成，產品狀態不得宣稱可交付。

真實姓名設定、群組實際分享、手機 PDF 分享與復原金鑰保存為 `OWNER-ONLY`；只回報 Owner 實際驗收結果，不以假名測試替代。

## 本機原型已執行檢查

- `node --check app.mjs`：PASS。
- `node scripts/self-check.mjs`：PASS；涵蓋日期邊界、週次分母、合成名冊去重、未知項目阻擋與常見日期格式判斷。
- 本機 HTTP 瀏覽器 smoke test：PASS；貼上入口、每日預覽／確認、摘要複製回饋、週次／個人切換及 390px 版面無水平溢位。
- 以上只證明假名／合成資料原型；登入、加密、跨裝置同步、群組唯讀與正式 PDF 仍為 `SKIPPED` 或 `OWNER-ONLY`。
